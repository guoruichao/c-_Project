using System;
using System.Collections.Generic;
using System.Text;

namespace libNet.Enum
{
    public enum DisconnectType
    {
        Normal,     // disconnect normally
        Timeout,    // disconnect because of timeout
        Exception   // disconnect because of exception
    } 
}
